[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["cd paginadedisparomindflow/pagina-de-disparo && PUPPETEER_SKIP_DOWNLOAD=true npm install"]

[phases.start]
cmd = "cd paginadedisparomindflow/pagina-de-disparo && npm start"
